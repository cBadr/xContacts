# Leave empty for local dev (Vite proxies /api to http://localhost:5174).
# In production (Vercel), set this to your backend's public URL, e.g.:
# VITE_API_URL=https://xcontacts-api.yourdomain.com
VITE_API_URL=
